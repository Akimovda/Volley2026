# Architecture — Volley

## 1. Общий обзор

Volley — это веб-приложение для организации волейбольных мероприятий
с акцентом на:
- удобный UX
- минимальные барьеры входа
- контекстную проверку данных пользователя

Архитектура строится по принципу:
> **Аутентификация ≠ Бизнес-логика**

---

## 2. Технологический стек

- Backend: Laravel 12
- Auth: Jetstream + Fortify
- DB: PostgreSQL
- Frontend: Blade + Vite
- Styles: Tailwind (база) + project CSS (`volley.css`)
- Auth providers:
  - Telegram (primary)
  - VK ID (secondary)

---

## 3. Основные домены

### Пользователь (User)
- идентификация
- профиль
- анкета игрока
- способы входа

### Мероприятие (Event)
- требования к участникам
- запись / отмена записи
- статус участия

---

## 4. Слои приложения

### 4.1 Контроллеры
Контроллеры **тонкие** и не содержат бизнес-логики.

Примеры:
- `EventRegistrationController`
- `ProfileExtraController`
- `EventsController`

Задача:
- принять запрос
- вызвать сервис
- вернуть redirect / response

---

### 4.2 Сервисы (Business Logic)

Вся логика требований вынесена в сервисы.

Пример:
App\Services\EventRegistrationRequirements

Сервисы:
- не знают о HTTP
- не знают о UI
- работают с моделями

---

### 4.3 Модели

Eloquent используется как ORM и слой доступа к данным.

Ключевые модели:
- `User`
- `Event`

Pivot-таблицы:
- `event_registrations`

---

## 5. UX-паттерны

### 5.1 Проверка по действию
- ❌ не при логине
- ❌ не при регистрации
- ✅ при попытке действия (запись на мероприятие)

---

### 5.2 Pending-состояния
Контекст пользователя хранится в `session`:

- `pending_event_join`
- `pending_profile_required`

Это позволяет:
- возвращать пользователя в поток
- не засорять URL
- избегать сложных FSM

---

## 6. Расширяемость

Архитектура допускает:
- добавление команд
- лимиты участников
- роли (организатор / игрок)
- платные мероприятия

Без изменения базовых принципов.

---

## 7. Ключевые принципы

- Контроллеры тонкие
- Логика в сервисах
- UX важнее формальной строгости
- Минимум блокировок
- Один пользователь — один аккаунт
- 
## 9 🧩 Архитектура: полное удаление пользователя (User Purge)
Цель
Обеспечить полное и безопасное удаление пользователя из системы, включая:
удаление файла аватара пользователя;
удаление записи пользователя из таблицы users;
автоматическое удаление или обнуление всех связанных данных;
корректную повторную авторизацию через OAuth (Telegram / VK / Yandex) после purge.
📌 Ключевые принципы
Единый источник истины — база данных
Вся каскадная логика реализована через Foreign Key + ON DELETE.
Контроллер не знает обо всех связанных таблицах.
Только физическое удаление
Используется forceDelete()
SoftDeletes не применяются для purge.
OAuth-провайдеры не ломают повторный вход
После удаления пользователь может заново войти через Yandex / VK / Telegram.
Если email не приходит от OAuth — создаётся безопасный surrogate email.
🗂 Связи в базе данных
Все таблицы, где пользователь фигурирует как user_id, должны иметь FK:
Таблица	Колонка	ON DELETE
account_link_audits	user_id	CASCADE
account_link_audits	linked_from_user_id	SET NULL
account_link_codes	user_id	CASCADE
account_link_codes	consumed_by_user_id	SET NULL
account_links	user_id	CASCADE
event_registrations	user_id	CASCADE
organizer_requests	user_id	CASCADE
organizer_requests	reviewed_by	NO ACTION
organizer_staff	organizer_id	CASCADE
organizer_staff	staff_user_id	CASCADE
user_beach_zones	user_id	CASCADE
user_classic_positions	user_id	CASCADE
admin_audits	actor_user_id	CASCADE
📌 Важно
Если в будущем появятся новые таблицы с user_id,
ничего менять в коде не нужно — достаточно правильно задать FK.
🛠 Миграции
Исправление правил удаления FK
Файл:
database/migrations/2026_01_12_100551_fix_user_fk_delete_rules_for_purge.php
Назначение:
привести все внешние ключи к корректным правилам:
CASCADE — для данных, принадлежащих пользователю;
SET NULL — для ссылочных/аудитных полей.
После добавления миграции:
php artisan migrate
🧑‍💻 Логика purge (AdminUserController)
Файл:
app/Http/Controllers/Admin/AdminUserController.php

Алгоритм purge
1. Проверка:
   - нельзя удалить самого себя
   - confirm=yes обязателен

2. Удаление файла профиля:
   - storage/public/{profile_photo_path}

3. Удаление пользователя:
   - forceDelete()

4. Все связанные записи:
   - удаляются БД автоматически (FK cascade)

5. Аудит:
   - admin_audits (action: user.delete.purge)
Ключевой принцип
Контроллер НЕ удаляет вручную связанные записи.
$user->forceDelete();
Если FK настроены корректно — этого достаточно.
🔐 OAuth и повторный вход после purge
Проблема
После purge пользователь:
заходит через Yandex;
OAuth отдаёт id, но не всегда email;
БД требует users.email NOT NULL.
Решение (YandexAuthController)
Файл:
app/Http/Controllers/Auth/YandexAuthController.php
Гарантия email при создании пользователя
$email = $yaUser->getEmail();

if (empty($email)) {
    $email = 'yandex_'.$yandexId.'@yandex.local';
}
📌 Аналогичная логика используется для:
Telegram (tg_{id}@telegram.local)
VK (vk_{id}@vk.local)
Это гарантирует:
уникальность;
соблюдение NOT NULL;
безопасный повторный вход.
🧪 Проверки после изменений
Рекомендуется выполнить:
php artisan test --filter=AccountLinkingTest
И вручную проверить:
Удаление пользователя из админки
Повторный вход через:
Yandex
VK
Telegram
Привязку аккаунтов после повторного входа
✅ Результат
Полное удаление пользователя работает стабильно
Нет "висячих" данных
OAuth-провайдеры не ломают повторную регистрацию
Архитектура масштабируется без правок контроллеров

## Purge (full delete)
Полное удаление пользователя реализовано через:
- удаление profile_photo_path (Storage::disk('public')->delete)
- forceDelete пользователя
- каскадное удаление зависимостей на уровне БД (FK + ON DELETE CASCADE/SET NULL)

См. также: docs/security.md и docs/db.md

