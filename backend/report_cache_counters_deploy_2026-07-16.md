# Деплой выпила кеш-счётчиков на прод (коммиты a9c73ab, 6550b1f, 78aa4ff + docs)

Дата: 2026-07-16, ~21:40-21:50 UTC. Prod: `/var/www/volleyplay/backend`, ветка `production`.

## 1. Деплой

- `git status` до мержа — чисто (не считая пред-существующих удалений двух старых report-файлов, не моих, не трогал).
- `git fetch origin` → `git merge origin/main --no-edit` — смёржены `a9c73ab..69f7e3a` без конфликтов: 9 PHP-файлов изменено, 1 удалён (`app/Events/OccurrenceStatsUpdated.php`), `CLAUDE.md` + новый `report_cache_counters_audit_2026-07-16.md`.
- `php -l` по всем 9 изменённым файлам — синтаксис ок.
- npm/route:cache/view:cache — не выполнялись (фронт/роуты/blade не менялись, как и предполагалось).
- `sudo systemctl reload php8.3-fpm` — выполнено вами вручную, подтверждено (`ExecReload` успешен).
- `sudo supervisorctl restart volleyplay-queue:*` — выполнено вами вручную, подтверждено по времени старта нового процесса воркера (`21:45:26`, `ps` показывает свежий `queue:work` для `volleyplay/backend`).

## 2. Смоук

### a) Виджет (главная проверка)

На проде оказался только один активный виджет (`user_id=1`, admin, 0 мероприятий) — непригоден для смоука. Создал временный виджет для организатора 260 (57 будущих occurrences, 13 будущих регистраций), проверил, удалил после теста.

- `curl .../api/widget/events?key=...` → occurrence 12254 («Классика игра», 16 июля): `slots_info.taken=13`.
- Сверка с live COUNT напрямую: `13` — совпадает точно.
- Сверка со старым кешем `event_occurrence_stats` для этой же occurrence: там тоже было `13` (кеш-строка существовала и случайно совпадала) — для ЭТОЙ конкретной occurrence разницы «было/стало» визуально нет, старый код тоже показал бы `13`.
- Более широкая проверка: на проде **только 97 из 496 occurrences (19.6%) вообще имеют строку в `event_occurrence_stats`** — остальные 80.4% в старом коде показали бы `taken=0` независимо от реальных записей. Поискал среди БУДУЩИХ occurrences с реальными регистрациями occurrence, где кеш-строки НЕТ (чтобы показать честное «было 0 → стало N») — не нашёл: на проде прямо сейчас каждая occurrence с реальной регистрацией уже имеет кеш-строку (это ожидаемо — `increment()` при создании регистрации раньше всегда её создавал; риск проявлялся при MASSOVYX отменах через 4 «дырявых» пути из аудита, которые НЕ удаляют кеш-строку, а могут ЗАНИЖАТЬ значение в НЕЙ, либо на occurrences без вообще какой-либо регистрации — там строки действительно нет, но и показывать там нечего). Проверил дрейф среди 2 будущих occurrences с кешем на проде — расхождений с live нет ни в одной.
- **Вывод по виджету**: новый код подтверждён корректным (точное совпадение с live COUNT на реальной occurrence с 13 регистрациями), но именно «было 0 → стало 13» на ЖИВЫХ данных прода продемонстрировать не удалось — на момент проверки нет occurrence, которая одновременно (а) имеет реальные регистрации и (б) не имела кеш-строки/имела неверную кеш-строку. Историческое подтверждение бага (было воспроизведено на dev с синтетической регистрацией: `taken=0` на старом коде → `taken=1` после отката к фиксу) остаётся в `report_cache_counters_audit_2026-07-16.md`.

### b) `/org/dashboard` организатора 260

Вызвал `OrgDashboardController::index()` напрямую (полный рендер blade, не только SQL):
- Страница рендерится без исключений, HTML 128956 байт.
- Блок «Эффективность ботов» найден в HTML, цифры не пустые: `avg_with_bots≈0.66, avg_without_bots≈4.04, occurrences_with_bots=5, occurrences_without_bots=92`.
- Соседний блок «Загрузка мероприятий» (не трогали в этом пакете) — тоже присутствует в HTML, не сломан.

### c) `GET /occurrences/{id}/stats`

```
curl .../api/occurrences/12254/stats → {"registered_total":13}
```
Совпадает с live COUNT (13) точно. 200 OK.

### d) Функциональная проба: запись + отмена

Создал изолированное синтетическое мероприятие (`[SMOKE-TEST] deploy verify`, event 397/occ 12392, НЕ трогал никакие реальные события), прогнал через реальные контроллеры (`EventRegistrationController::storeOccurrence()` → `destroyOccurrence()`) с реальным тестовым пользователем:
- Регистрация — `OK`, `is_cancelled=false`.
- Отмена — `OK`, `is_cancelled=true, cancelled_at` проставлен.
- Ни одного исключения. Синтетика удалена полностью после проверки.

### e) Логи

- `laravel.log` — новых ошибок, связанных с деплоем, нет. Единственные записи в хвосте — предсуществующие (11:07, 16:11, задолго до деплоя в 21:4x) варнинги о недоступности Telegram/аватарки, не связаны.
- `queue-worker.log` — после restart воркера (21:45:26) прошли реальные джобы: `ReleaseExpiredPaymentsJob`, `AutoUnconfirmBookingJob` (штатные по расписанию) и, что важнее, **`NotifyOrganizerRegistrationJob` (×2) + событие `PlayerJoinedOccurrence`** — это джобы, порождённые НАШИМ функциональным тестом (пункт d) в 21:49:12, все `DONE` без ошибок. Это прямое подтверждение, что перезапущенный воркер с новым кодом (без `increment()/decrement()`) обрабатывает реальные события записи/уведомлений корректно.
- `BotAssistantJob` (использует `BotAssistantService`, из которого убраны 2 вызова `increment/decrement`) в логе виден только ДО restart (21:30:05, старый код, тоже без ошибок) — следующий штатный запуск (каждые 30 мин) будет уже на новом коде; не ждал его отдельно, т.к. код синтаксически корректен, логика проверена на dev в регрессии, а сам вызов сервиса физически удалён (не может упасть на несуществующем вызове).

## 3. Итог

Все пункты смоука пройдены, ошибок не найдено. Единственное отклонение от плана — пункт (a) не смог продемонстрировать «было 0 → стало N» на ЖИВЫХ прод-данных (только на синтетике, как и на dev), т.к. на момент проверки прод не содержит occurrence с одновременно реальными регистрациями и пустой/неверной кеш-строкой — это хорошая новость (сейчас никто не видит неверных чисел в виджете), но не идеальная демонстрация. Логика подтверждена математически точным совпадением (13=13=13) на реальной occurrence и synthetic-тестом на dev в прошлой сессии.

Миграции `DROP` колонки/таблицы — как и договаривались, НЕ делал, в этот деплой не входили.
