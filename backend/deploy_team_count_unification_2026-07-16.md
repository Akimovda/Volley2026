# Деплой: единый подсчёт команд (countRegisteredTeams) — прод, 2026-07-16

Коммиты: `09fa7cb..a9aa449` (main → production).

## Ход деплоя

1. `/var/www/volleyplay/backend`: pre-merge статус чистый (только 2 старые незакоммиченные удалённые .md-заметки, не мои — не трогал), `git fetch` показал ровно 4 ожидаемых коммита, `git merge origin/main --no-edit` — без конфликтов, затронуты ровно 5 файлов (`CLAUDE.md`, `OrgDashboardController.php`, `EventRegistrationGuard.php`, `TournamentTeamService.php`, `players.blade.php`).
2. `php -l` на всех трёх PHP-файлов — чисто.
3. `php artisan view:clear && php artisan view:cache` — blade менялся, пересобралось без ошибок. route/config не трогал (не менялись), npm не запускал (JS/Vite не менялись).
4. Вы выполнили `sudo systemctl reload php8.3-fpm` и `sudo supervisorctl restart volleyplay-queue:*` — проверил: `php8.3-fpm` active, prod queue-worker процесс (PID 4033635) стартовал заново (38 сек аптайм на момент проверки).

## Смоук на живых прод-данных

| Кейс | Дашборд организатора | Availability/seatline (`Guard::check()`) | Страница `/events/{id}` |
|---|---|---|---|
| **a. Event 395** (organizer 260) | `avg_load_pct = 100%` (было 125%) | `registered=4 max=4 remaining=0 reserve=1` (было 5/4) | «Команд: 4 / 4» + «1 в резерве», команда Постоваловой на месте в резерве — **без изменений**, как и ожидалось |
| **b. Event 388** (team_classic, organizer 260) | — (не проверял отдельно, числа не зависят от фикса) | `registered=4 max=4 remaining=0 reserve=0` | — | 
| **c. Event 376** (лига ББЛ, organizer 276, occurrence 12194 — round 3, 14 команд всего) | `avg_load_pct = 20.8%` (агрегат по всем occurrences за 3 мес, ожидаемо низкий — сезон только начался, не все туры укомплектованы) | `registered=6 max=8 remaining=2 reserve=8` | «Команд: 6 / 8» + «8 в резерве» — **совпадает во всех трёх местах** |

Для **b** сравнение «не изменились против вчерашних»: вчера (до фикса) 388 показывал 4/4 без резерва — сегодня те же 4/4/резерв=0. Совпадает, фикс не затронул случаи без резерва.

Для **c** взял occurrence 12194 (round 3 события 376) вместо round 7 (12198, использовался на dev) — там оказалось больше команд (14 против 8), нагляднее показывает работу лигового резерва: **6 в зачёте + 8 в резерве = 14 всего**, дивизион «ББЛ 1» (`league_id=10`, `max_teams=8`) — ровно по формуле, все три места (canonical-метод внутри Guard, дашборд, страница) согласованно показывают 6/8 + 8 в резерве.

## Логи

- `laravel.log`: единственные `ERROR`-записи в файле — от МОЕГО собственного вчерашнего диагностического скрипта `/tmp/diag_395_teams.php` (опечатка в имени колонки `team_id` вместо `event_team_id`, стектрейс это прямо показывает — `#6 /tmp/diag_395_teams.php(16)`). К деплою/приложению отношения не имеют, файл уже удалён из `/tmp`. Свежих ошибок после merge/reload/restart — нет.
- `queue-worker.log`: после рестарта воркер поднялся, штатные джобы (`ReleaseExpiredPaymentsJob`, `AutoUnconfirmBookingJob`, `BotAssistantJob`) выполняются и завершаются `DONE` без сбоев (06:45:02 — сразу после рестарта).

## Итог

Все 4 коммита на проде, три места подсчёта команд (дашборд, seatline/карточка, страница события) согласованы на всех проверенных кейсах — с резервом (событийным на 395, лиговым на 376) и без (388). Мёртвый код (`getLeagueReserveTeamIds()`) удалён и на dev, и теперь на проде. Откатывать не потребовалось.
