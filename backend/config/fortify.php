<?php

use Laravel\Fortify\Features;

return [

    /* =======================================================================
     | Fortify Guard
     | ======================================================================= */
    'guard' => 'web',

    /* =======================================================================
     | Fortify Password Broker
     | ======================================================================= */
    'passwords' => 'users',

    /* =======================================================================
     | Username / Email fields
     | ======================================================================= */
    'username' => 'email',
    'email' => 'email',

    /* =======================================================================
     | Lowercase usernames
     | ======================================================================= */
    'lowercase_usernames' => true,

    /* =======================================================================
     | Home path after successful auth / password reset
     | Важно: /dashboard у вас под 'verified', поэтому безопаснее вести на /events
     | ======================================================================= */
    'home' => '/events',

    /* =======================================================================
     | Routes prefix / domain
     | ======================================================================= */
    'prefix' => '',
    'domain' => null,

    /* =======================================================================
     | Routes middleware
     | ======================================================================= */
    'middleware' => ['web'],

    /* =======================================================================
     | Rate limiting keys (см. FortifyServiceProvider)
     | ======================================================================= */
    'limiters' => [
        'login' => 'login',
        'two-factor' => 'two-factor',
    ],

    /* =======================================================================
     | Register view routes
     | ======================================================================= */
    'views' => true,

    /* =======================================================================
     | Features
     | ======================================================================= */
    'features' => [
        Features::registration(),
        Features::resetPasswords(),
        // Features::emailVerification(),
        Features::updateProfileInformation(),
        Features::updatePasswords(),

        Features::twoFactorAuthentication([
            'confirm' => true,
            'confirmPassword' => true,
            // 'window' => 0,
        ]),
    ],

];
