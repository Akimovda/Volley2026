<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Финализация анонсов при завершении мероприятия
    |--------------------------------------------------------------------------
    | Команда events:finalize-announcements редактирует уже опубликованные посты
    | (кнопка «Записаться!» → «🏁 Мероприятие завершено»), когда occurrence
    | завершилась (ends_at < now). Чтобы не разбудить редактированием сотни
    | старых постов при первом деплое фичи — финализируются только те occurrences,
    | что завершились не раньше этого количества часов назад.
    */
    'finalize_announcement_max_age_hours' => env('CHANNEL_FINALIZE_MAX_AGE_HOURS', 6),

];
